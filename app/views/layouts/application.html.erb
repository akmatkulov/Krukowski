<!DOCTYPE html>
<html>
  <head>
    <title>Krukowski</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <nav class="nav">
            <ul class="nav nav-underline">
              <li class="nav-item">
                <%= link_to 'Home', root_path, class: "nav-link link-light" %>
              </li>
              <li class="nav-item">
                <%= link_to 'Questions', questions_path, class: "nav-link" %>
              </li>
               <li class="nav-item">
                <%= link_to 'New', new_question_path, class: "nav-link" %>
              </li>
            </ul>
           
        </nav>  
      </div>
    </header>

    <div class="container">
      <div class="row">
        <% flash.each do |key, value| %>
            <%= tag.div value, class: "alert alert-#{key} mt-3", role: "alert" %>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
